# Sync từ repo `tf4-phase3-repo` — 16/07/2026

> Research nằm ở Downloads chỉ để chỉnh sửa.  
> Nguồn sự thật kỹ thuật: git repo `D:\source code\tf4-phase3-repo` (branch `main`).

## Meta quét

| Mục | Giá trị |
|---|---|
| Branch | `main` (tracking `origin/main`) |
| HEAD lúc quét | `40cd1ff` — docs incident async-order (#214) |
| Evidence Mandate-04 | `docs/evidence/mandate-04-forensic/` (rename #229: `af757d4`, 09:17 +07) |
| Evidence bổ sung gần nhất | #223 `5e31169` (08:05 +07) — framework forensic files |

## Delta so với bản research trước (cần phản ánh vào file 00–05)

### 1. CloudTrail đã đẩy sang CloudWatch Logs (mới / chưa nhấn mạnh đủ)

Evidence `aud-17.1-cloudtrail-config.md` + status:

- `LogFileValidationEnabled = true`
- `CloudWatchLogsLogGroupArn` = `/aws/cloudtrail/tf4-general-cloudtrail`
- Delivery CWL gần đây (15/07) OK

→ **P1 không chỉ EKS:** có thể dùng **CloudWatch Logs Insights** cho CloudTrail gần đây, không chỉ `lookup-events` / Athena.

### 2. AWS Config đang recording (change trail thứ 3)

`aud-17.1-aws-config-status.md`: recorder `tf4-config-recorder` = `recording: true`, delivery Success.

→ Sơ đồ nguồn log phải gồm **Config history/snapshot** (không chỉ CT + EKS).

### 3. Playbook forensic **lệch evidence** (debt docs)

`docs/audit/runbooks/forensic-playbook-timeline.md` §5 vẫn viết:

> CloudTrail `LogFileValidationEnabled` hiện là `false` — ticket AUDIT-011…

Trong khi AUD-17.1/17.3 xác nhận `true` + validate-logs 34/34 digest.

→ Backlog: **cập nhật playbook** (bỏ gap cũ; nêu Object Lock COMPLIANCE + validate-logs).

### 4. OpenSearch — persistence đã bật nhưng **đầy đĩa / không đủ cho audit ingest**

| Trước (scan/ADR cũ) | Hiện tại (CDO08 week2 evidence) |
|---|---|
| ADR-009 / scan: `persistence.enabled: false` | PVC `opensearch-opensearch-0` **8Gi Bound** (`jaeger_persistence_evidence.md`) |
| Security plugin tắt | **Vẫn tắt** (`DISABLE_SECURITY_PLUGIN=true`, ADR-009 Accepted tạm) |
| — | Incident disk watermark ~90%: Jaeger không ghi được (`JAEGER-OPENSEARCH-INCIDENT-OPTION-C.md`); đề xuất tăng **20Gi** + giảm retention |

→ **Củng cố quyết định:** không ingest audit trail vào OpenSearch ở P0/P1. P2 chỉ sau capacity + security.

### 5. Backlog audit log **đã có sẵn** trong team plan (nên gắn task mới vào)

| Repo ref | Nội dung | Liên hệ task “phân tích log” |
|---|---|---|
| `DELEGATED_TASKS_P0.md` Task 3.1 | OpenSearch ISM | Retention vòng đời index (không thay Object Lock) |
| `DELEGATED_TASKS_P0.md` Task 3.2 | Grafana Audit Dashboard (401/403) | UI soi bất thường — mở rộng sang forensic saved queries |
| `TEAM_ASSIGNMENT.md` Member 7–8 | Log Retention + Dashboard Tracker | Role sẵn cho epic phân tích log |
| `JIRA_TASKS.md` Task 7–8 | Review ISM + nghiệm thu Grafana Audit Dash | Có thể extend DoD thay vì invent epic hoàn toàn mới |

### 6. Prefix S3 chính xác (cho Athena PoC)

Từ `aud-17.1-cloudtrail-config.md`:

- Bucket: `tf4-cloudtrail-logs-bucket-511825856493`
- Prefix: `AWSLogs/511825856493/CloudTrail/`
- EKS bucket: `tf4-eks-audit-logs-511825856493` (Firehose ACTIVE)

### 7. Những gì **không** đổi (xác nhận lại)

- Mandate-04 SoT vẫn S3 Object Lock COMPLIANCE 90d (CT + EKS) — PASS
- Drill AUD-17.2: 3/3 PASS bằng CLI
- Firehose `tf4-eks-audit-logs-firehose` ACTIVE
- Chưa thấy Athena / ELK / Datadog / Loki-as-audit trong repo

## File research đã cập nhật theo sync này

- `00`, `01`, `02`, `03`, `04`, `05`, `README` — phản ánh delta trên
- File này (`07`) — nhật ký sync để lần sau diff tiếp

## Lệnh gợi ý lần sync sau

```powershell
cd "D:\source code\tf4-phase3-repo"
git fetch origin
git log --oneline origin/main -20
git log --oneline --since="2026-07-16" -- docs/evidence/mandate-04-forensic docs/audit docs/cdo08
```
